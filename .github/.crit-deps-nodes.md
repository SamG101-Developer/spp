# Notes

- Don't upgrade `ankerl/unordered-dense` to 4.11; 4.10 is safe.
  - **https://gcc.gnu.org/bugzilla/show_bug.cgi?id=127193** needs fixing.
- Don't let `refresh-pins.sh` move `stringzilla`; it resolves `HEAD`, which is the default branch.
  - The pin is on `main-v6`, the only branch whose `install()` exports a `CMake` package.
