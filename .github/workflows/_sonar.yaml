name: sonar

on:
  workflow_call:
    inputs:
      build-type:
        required: false
        type: string
        default: "Release"
    secrets:
      SONAR_TOKEN:
        required: true

jobs:
  sonar:
    name: SonarQube Cloud
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
    steps:
      # Sonar needs full history for its new-code / blame analysis, which is also
      # what the mtime restore wants, so the default checkout is right here.
      - uses: ./.github/actions/checkout

      - name: Set Up Toolchain
        uses: ./.github/actions/setup-toolchain
        with:
          compiler: gcc
          version: "16"
          cache-key-suffix: sonar

      # Shares the release variant's tree: Sonar only adds
      # -DCMAKE_EXPORT_COMPILE_COMMANDS, which does not invalidate any objects.
      - name: Restore build tree
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: build
          key: build-ubuntu-latest-gcc16-release-${{ github.sha }}
          restore-keys: |
            build-ubuntu-latest-gcc16-release-

      - name: Configure
        uses: ./.github/actions/configure-cmake
        with:
          build-type: ${{ inputs.build-type }}
          extra-flags: -DCMAKE_EXPORT_COMPILE_COMMANDS=ON

      - name: Build
        run: cmake --build build --parallel

      - name: SonarQube Scan
        uses: sonarsource/sonarqube-scan-action@22918119ff8e1ca75a623e15c8296b6ea4fbe28f
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: https://sonarcloud.io
