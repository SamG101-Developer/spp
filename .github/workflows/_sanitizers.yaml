name: sanitizers

on:
  workflow_call:
    inputs:
      configs:
        required: true
        type: string
      os:
        required: false
        type: string
        default: "ubuntu-latest"

permissions:
  contents: read

jobs:
  sanitize:
    name: ${{ matrix.config }}
    runs-on: ${{ inputs.os }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        config: ${{ fromJSON(inputs.configs) }}
    env:
      ASAN_OPTIONS: "abort_on_error=1:halt_on_error=1:detect_leaks=1:strict_string_checks=1"
      UBSAN_OPTIONS: "print_stacktrace=1:halt_on_error=1"
      TSAN_OPTIONS: "halt_on_error=1:second_deadlock_stack=1"

    steps:
      - name: Harden runner
        uses: step-security/harden-runner@v2
        with:
          egress-policy: audit
      - uses: actions/checkout@v7
      - name: Set up toolchain
        uses: ./.github/actions/_setup_toolchain
        with:
          compiler: gcc
          version: "16"
          cache-key-suffix: san-${{ matrix.config }}

      - name: Restore build tree
        uses: actions/cache/restore@v6
        with:
          path: build
          key: build-san-${{ inputs.os }}-${{ matrix.config }}-${{ github.sha }}
          restore-keys: |
            build-san-${{ inputs.os }}-${{ matrix.config }}-

      - name: Configure (${{ matrix.config }})
        uses: ./.github/actions/_configure_cmake
        with:
          build-type: Debug
          sanitizer: ${{ matrix.config }}

      - name: Build
        run: cmake --build build --parallel

      - name: Test under ${{ matrix.config }}
        run: ctest --test-dir build --output-on-failure --no-tests=error

      - name: Upload sanitizer logs on failure
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: san-${{ matrix.config }}-logs
          path: |
            build/Testing/Temporary/LastTest.log
          if-no-files-found: ignore

      - name: Save build tree
        if: always()
        uses: actions/cache/save@v6
        with:
          path: build
          key: build-san-${{ inputs.os }}-${{ matrix.config }}-${{ github.sha }}
